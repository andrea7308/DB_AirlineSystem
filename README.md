# DB_AirlineSystem
Make sure to create a virtual environment using: python3 -m venv venv, and to install the following dependencies to run.

use pip install <name> (replace name with one of 5 in list)
1. flask
2. pymysql
3. python-dotenv
4. pandas
5. plotly
